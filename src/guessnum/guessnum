if [ $1 -gt $2 ] 2>/dev/null
    then echo "$1 > $2"
    elif [ $1 -eq $2 ] 2>/dev/null
        then echo "$1 = $2"
        elif [ $1 -lt $2 ] 2>/dev/null
            then echo "$1 < $2"
            else
                echo "That's not a number lol"
fi
echo usage:guessnum $1 $2
